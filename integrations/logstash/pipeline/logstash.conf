input {
    beats {
        port => 6000
    }
}

filter {
    # You can keep this empty for now — no transformations yet.
}

output {
    # 1️⃣ Still print to console (for debugging)
    stdout {
        codec => rubydebug
    }

    # 2️⃣ Send full JSON body to your FastAPI /test-logstash endpoint
    http {
        url => "http://predictor_api:8000/test-logstash"
        http_method => "post"
        format => "json"
        content_type => "application/json"
    }
}