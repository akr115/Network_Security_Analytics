# Temporary Logstash Configuration (logstash.conf)

input {
    beats {
        port => 6000 # Ensure this matches the port in filebeat.yml
    }
}

filter {
    # Keep this empty or comment out the existing processing
    # You are only verifying ingestion, not transformation yet.
}

output {
    # 🚨 TEMPORARY: Output to console for verification
    stdout {
        codec => rubydebug
    }
    # Comment out the Elasticsearch output for now
    # elasticsearch {
    #     hosts => ["http://elasticsearch:9200"]
    #     index => "suricata-anomalies-%{+YYYY.MM.dd}"
    # }
}