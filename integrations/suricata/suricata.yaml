%YAML 1.1
---

# ========== BASIC VARIABLES ==========
vars:
  address-groups:
    # Internal private ranges used in the dataset
    HOME_NET: "[192.168.10.0/24, 172.16.0.0/16, 10.0.0.0/8, 205.174.165.68]"
    # Everything that's not HOME_NET
    EXTERNAL_NET: "!$HOME_NET"

# ========== RUNMODE ==========
runmode: autofp  # automatic flow-based multithreading

# ========== INPUT SOURCE ==========
pcap:
  - interface: pcap-file
    checksum-checks: auto

# ========== RULES ==========
default-rule-path: /etc/suricata/rules
rule-files:
  - suricata.rules  # can be empty if you just want flow/stat logs

# ========== OUTPUTS ==========
outputs:
  - eve-log:
      enabled: yes
      filetype: regular
      filename: eve.json
      types:
        - alert
        - flow
        - http
        - dns
        - tls
        - stats

  - fast:
      enabled: no

logging:
  default-log-level: info
  outputs:
    - console:
        enabled: yes
